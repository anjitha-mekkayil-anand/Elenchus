# Anthropic API key — required for real model calls (NF-3)
ANTHROPIC_API_KEY=your-api-key-here

# Optional: override the base directory for pages/, sources/, ingests/, elenchus.db
# ELENCHUS_BASE_DIR=/path/to/knowledge-base
